version: 2.1

jobs:
  optional:
    parameters:
      runit:
        type: string
    machine: true
    steps:
      - when:
          condition: <<parameters.runit>>
          steps:
            - run: echo \"runit\"
workflows:
  version: 2
  testthis:
    jobs:
      - optional
